apiVersion: v1
kind: Service
metadata:
  name: node-api-poc-bg-active
  namespace: devsecops-bg
spec:
  selector:
    app: node-api-poc-bg
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
